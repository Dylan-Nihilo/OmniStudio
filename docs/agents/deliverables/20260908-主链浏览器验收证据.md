# 主链浏览器验收证据

## 本次运行

- 日期：2026-09-09
- 分支：`feature/sflynnn-omni`
- 前端：`http://127.0.0.1:3008`
- 命令：`\.venv\Scripts\python.exe scripts/run_acceptance.py --browser chromium --url http://127.0.0.1:3008 --output .artifacts/acceptance/task5e-20260909`
- Fixture：`tests/fixtures/acceptance_fixture.json`
- Fixture SHA-256：`5f019ea656ea1b7203753318216077c1f64d9473e8547fd6dad00fb67214d540`

## 结果

`run.json` 显示 8 个步骤全部通过，0 个失败，0 个阻塞，事件数 177：

- Workspace
- Source
- Project
- Script
- Cast
- Storyboard
- Tasks
- Assembly / Export

证据目录：`.artifacts/acceptance/task5e-20260909/`

- `run.json`：步骤状态、URL、fixture 摘要和截图路径
- `events.jsonl`：请求、响应、console、fixture 媒体和失败任务记录
- `01-workspace.png` 至 `08-assembly.png`：每一步完整页面截图
- 对应 `.html` 文件：每一步 DOM 快照

固定 fixture 已验证包含 3 个章节、2 个 Episode、5 个场景、可读图片/视频/音频，以及失败任务；媒体响应均由本地前端服务返回 200。

## 边界与未完成验收

本次脚本使用只读 fixture 路由，除编辑租约外拒绝写请求。因此本证据只证明主链导航、页面地标、固定媒体和错误任务状态可见，不证明真实后端导出已成功写入文件。

仍需在真实后端/可写测试数据库完成：

- 保存分辨率、帧率、CRF、编码速度、音频码率和软字幕设置
- 运行导出预检后合片、下载，并用 `ffprobe` 核对视频/音频/字幕流
- owner/editor/viewer 权限、跨 Workspace 媒体访问和登录过期
- 取消、重试、页面离开、服务重启后的生产任务恢复

## 本轮恢复回归

- 提交：`6638cd9 fix: record production job recovery events`
- 后端回归：`.venv\\Scripts\\python.exe -m pytest -q --basetemp=.tmp-pytest-final-head` → `654 passed`
- 前端回归：`npm run test` → `40 files / 278 tests passed`
- 类型与构建：`npm run typecheck`、`npm run build` 均通过
- 浏览器复验：`.artifacts/acceptance/task6-recovery-20260909` → 8/8 主链步骤通过
- 恢复语义：可恢复 JobItem 保持 `processing` 供 adapter 重放，并新增 `recovered` 事件；未知类型仍进入 `failed` 并记录 `RECOVERY_UNAVAILABLE`。

## Task 6 角色权限回归

- 后端角色契约：`owner`、`editor`、`viewer`，兼容旧 `role=owner/member` 数据；旧成员默认 `member`，Owner 自动回填 `owner`。
- 邀请创建、邀请注册、已有成员接受邀请和成员角色更新均持久化 `access_role`。
- viewer 对业务写请求返回 `AUTH_VIEWER_READ_ONLY`；editor 可编辑项目，但删除项目仍返回 `AUTH_OWNER_REQUIRED`。
- 自动化证据：
  - `.venv\\Scripts\\python.exe -m pytest tests/test_workspace_roles.py tests/test_workspace_collaboration.py tests/test_auth_setup.py tests/test_auth_route_guard.py -q --basetemp=.tmp-pytest-role-regression` → `26 passed`
  - `.venv\\Scripts\\python.exe -m pytest tests/test_w2_project_api.py::test_editor_can_edit_project_but_cannot_delete_it -q --basetemp=.tmp-pytest-editor` → `1 passed`
  - `frontend\\npm run typecheck` → 通过；`frontend\\npm run test -- --run` → `40 files / 278 tests passed`
- 浏览器复验：`.artifacts/acceptance/task6-roles-20260909` → 主链 8/8 步骤通过。该脚本使用只读 fixture 路由，角色写入矩阵仍以后端真实 API 回归为准。

仍需在真实后端/可写测试数据库完成：

- owner/editor/viewer 的完整浏览器点击矩阵、跨 Workspace 媒体访问和登录过期；
- 运行导出预检后合片、下载，并用 `ffprobe` 核对视频/音频/字幕流。
