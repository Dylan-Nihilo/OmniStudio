version: 1
defaults:
  # Every default must point at a model the price book covers, or a fresh workspace would be
  # unable to generate anything the moment billing is switched on.
  model_settings:
    t2i_model: gpt-image-2
    i2i_model: gpt-image-2
    i2v_model: wan2.7-i2v
    r2v_model: wan2.7-r2v
    image_model: gpt-image-2
